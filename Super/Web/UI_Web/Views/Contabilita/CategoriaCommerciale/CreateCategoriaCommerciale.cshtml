@model Core_Web.Models.CreateCategoriaCommerciale
@{
    ViewBag.Title = "add categoriaCommerciale";
    Layout = string.Empty;
}
<div id="error_mssg" class="hidden"></div>
<form id="formDetails">
    <fieldset>
        <legend>CategoriaCommerciale</legend>
        <div class="editor-label">
            @Html.LabelFor(model => model.Description)*
        </div>
        <div class="editor-field">
            @Html.TextBoxFor(model => model.Description, new { @class = "validate[required]" })
        </div>
       
    </fieldset>
    @Html.HiddenFor(model => model.Id)
    @Html.HiddenFor(model=>model.CommitId)
    @Html.HiddenFor(model=>model.Version)
</form>
<script>

    
    $(document).ready(function () {
        $('.datepicker').datepicker({ dateFormat: "dd/mm/yy" });
        jQuery("#formDetails").validationEngine();
        

        viewModel.Accept = function (self) {

            jQuery('#formDetails').validationEngine({ scroll: false });
            if(jQuery('#formDetails').validationEngine('validate')) {

                var id = $('#Id').val();
                var commitId = $('#CommitId').val();
                var version = $('#Version').val();
                var description = $('#Description').val();
                var idCategoriaCommerciale = $('#IdCategoriaCommerciale').val();
              
                var url = "@Url.Action("CreateCategoriaCommerciale", "CategoriaCommerciale")";

             
                try 
                {
                    var command = new CreateCategoriaCommerciale(id, commitId, version,   description,idCategoriaCommerciale);

                    self.CreateCategoriaCommerciale(url, command);
                } catch(e) {
                    alert(e);
                }
            }
        };
        
       
        
    });
</script>
